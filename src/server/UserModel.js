// const Mongoose= require('mongoose');
// const Joi = require('@hapi/joi');
// const Joigoose = require('joigoose');

// const joiScheme = Joi.object({
//     name:Joi.string().allow('').default('loh'),
//     e_mal:Joi.string().allow('').default(''),
//     is_log:Joi.boolean().default(false),
// })

// const mongoseScheme = new Mongoose.Schema(Joigoose.convert(joiScheme));

// const mongooseScheme = Mongoose.model('user',mongoseScheme, 'user' );
